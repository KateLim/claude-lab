# 미래를 위한 배당 설계기 — 작업 인계 메모

노후 배당 목표를 역산하고 포트폴리오를 설계하는 **로컬 웹 서비스**. 파이썬 표준
라이브러리만 쓰고 프런트엔드는 빌드 단계가 없다. 설치할 것이 없다.

```
python3 server.py                              # http://127.0.0.1:8770
python3 -m unittest discover -s tests -t .     # 55개, 네트워크 없이 돎
python3 tools/selfcheck.py --start             # 엔드포인트 전체를 실제로 두드림
```

기능·계산식·데이터 파이프라인의 자세한 설명은 `README.md`에 있다. **이 문서는 코드를
읽어도 알 수 없는 것만** 적는다 — 왜 이렇게 만들었고, 무엇을 건드리면 안 되는지.

## 이 프로젝트의 출발점

`https://d35e21nev0myoj.cloudfront.net/` 를 로컬에서 돌게 만들어 달라는 요청으로
시작했다. 원본은 소스가 없으므로 **응답을 관찰해 계산식을 역산했다.**

- `tests/golden/plan_*.json`, `goal_base.json` — 원본 API의 실제 응답 17+1건
- `tests/golden/universe.json` — 원본이 계산한 36종목 지표
- `tests/golden/market_bundle.json` — 그때의 시장 데이터 스냅샷 (비교 기준으로 **고정**)

`tests/test_parity.py` 가 이 응답들과 맞춰본다. **백테스트·전망 시계열, 요약 지표,
필요 적립액, 목표 도달 시점은 같은 비중을 넣으면 부동소수점 오차(1e-9) 안에서
재현된다.** 이게 이 프로젝트의 핵심 자산이다 — 계산식을 고칠 때 여기가 깨지면
역산 결과를 잃는다.

원본과 의도적으로 다르게 만든 것은 README의 「참조 서비스와 다른 점」에 적어 뒀다.

## 건드리면 안 되는 것 (깨지면 조용히 깨진다)

### 1. 정합성 테스트의 기준은 고정 스냅샷이다

`test_parity.py` 는 `data/market.json`(갱신되는 캐시)이 아니라
`tests/golden/market_bundle.json` 을 읽는다. 실행 중에 데이터를 갱신해도 테스트가
흔들리지 않아야 **회귀와 데이터 변화를 구분**할 수 있다. 이 파일은 갱신하지 않는다.

### 2. ETF 비중 제약은 원본에 없다

`portfolio.build(..., etf_ratio)` 기본값은 `0.6`(ETF 코어)이지만, 정합성 테스트는
`etf_ratio=None`(제약 없음)으로 원본 경로를 검증한다. 제약 로직을 고칠 때 `None`
경로를 없애면 안 된다.

### 3. 데이터 출처를 필드 단위로 기록한다

야후가 막히면 **가격은 나스닥/네이버에서 최신으로, 배당은 캐시에서** 가져온다.
종목마다 `price_source` / `dividend_source` 를 남기고 화면에 그대로 띄운다.
옛 숫자를 새 숫자처럼 보여주지 않는 것이 이 설계의 요점이다. 출처 표기를 지우면
안 된다.

### 4. 경로 접두어 독립성

브라우저 code-server 의 `/proxy/8770/` 뒤에서도 열려야 한다. 그래서:

- HTML은 자산을 **상대 경로**로 참조한다 (`href="styles.css"`)
- 라우트는 **한 칸 깊이만** 쓴다 (`/plan`, `/ticker?symbol=MAIN`)
- `app.js` 는 자기 `<script>` 태그의 URL에서 기준 경로를 계산한다
- 자산으로 보이는 경로는 SPA 폴백 대신 **404**

깨지는 방식이 조용하다 — CSS 자리에 `index.html` 이 오면 화면만 밋밋해지고 콘솔에는
아무것도 안 남는다. `tests/test_api.py` 의 `BasePathIndependence` 가 규칙을 지킨다.

### 5. 환율은 상수배에 불변이다

매수 단가·평가액·배당이 모두 환율에 선형이라, 환율에 상수를 곱하면 그대로 상쇄된다.
그래서 백테스트 결과는 환율의 **절대 수준에 불변**이고 상대 변화만 남는다. 절대 환율을
받을 수 있으면 원화 환산 표시에 쓰고(`fx_absolute: true`), 지수만 있으면 원화 환산을
표시하지 않는다 — 모르는 값을 지어내지 않는다.

### 6. 데이터 캐시는 git 에 없다

`data/market.json` 은 갱신되는 캐시라 `.gitignore` 에 있다. 저장소에는
`data/market.seed.json` 만 있고, 캐시가 없으면 `dataset.load_pack()` 이 seed 를
복사해서 시작한다. 새로 클론한 머신에서도 첫 실행이 그냥 된다.

## 이 환경에서만 겪는 것

- **야후 파이낸스가 429로 막힌다.** 데이터센터 IP를 차단한다. 그래서 갱신하면 가격은
  나스닥(미국 10년) · 네이버(국내 20년), 배당은 캐시, 환율은 ECB(Frankfurter)로
  채워진다. 일반 네트워크에서는 야후 한 번으로 전부 온다.
  - 배당까지 최신으로 받으려면 `ALPHAVANTAGE_API_KEY` 를 넣으면 된다 (무료 키는
    하루 호출 수가 적어 `--only` 로 나눠 받는 편이 낫다).
- 갱신은 한 공급자가 2회 연속 실패하면 그 공급자를 버린다. 없으면 36종목마다
  재시도하며 몇 분을 버린다 (208초 → 76초).
- `make` 가 없는 머신이 있다. `Makefile` 은 단축키일 뿐이고 README의 `python3` 명령이
  본선이다.

## 앞으로 할 만한 것

- **국내 상장 ETF 추가** — 지금 유니버스의 ETF는 전부 미국 상장이라 ETF 비중이 미국
  비중을 넘을 수 없다. 다만 배당 이력을 주는 공급자에 닿아야 추가할 수 있다.
  닿지 않으면 새 종목은 `ttm_yield=0` 이 되어 "배당수익률 0% < 최소 기준 1%" 로
  영구 제외된다.
- MDD·총수익 CAGR을 일별 데이터로 계산 (지금은 월말 종가 기준이라 원본과 종목당
  최대 10%p 차이)
- 세금 모델 정교화 (지금은 배당 원천징수율만 단순 적용)

## 코드 스타일

- 주석은 **왜**를 적는다. 무엇을 하는지는 코드가 말한다.
- 한국어 UI. 조사는 `narrative.particle()` 로 붙인다 (알파벳은 한국어로 읽은 소리로
  판단 — 엘·엠·엔·알만 받침이 남는다: `MAIN은`, `ETF는`, `KT는`).
- 새 계산식을 넣으면 테스트를 같이 넣는다. 이 프로젝트의 테스트는 두 종류다 —
  원본과의 정합성, 그리고 우리가 새로 정한 규칙.
