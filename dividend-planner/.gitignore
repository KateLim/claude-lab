__pycache__/
*.pyc
.DS_Store
tests/_tmp_*

# 시장 데이터 캐시. 저장소에는 data/market.seed.json 만 넣는다 —
# 캐시는 갱신될 때마다 바뀌어서 머신마다 다른 diff 를 만든다.
data/market.json
data/*.tmp

# Claude Code 세션 메모 (자체 .gitignore 도 있지만 의도를 드러내 둔다)
.remember/
